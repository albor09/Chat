import Centered from "../Centered";
import Spinner from "./style";

const CircleLoading = () => {
  return (
    <Centered>
      <Spinner></Spinner>
    </Centered>
  );
};

export default CircleLoading;
